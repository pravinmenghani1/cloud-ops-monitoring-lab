# Copy this file to terraform.tfvars and modify the values as needed

aws_region = "us-east-1"
instance_name = "docker-nginx-instance"
s3_bucket_name = "ssm-docker-install-logs"

# Change this to your IP address for better security
# You can get your IP by running: curl ifconfig.me
allowed_cidr_blocks = ["0.0.0.0/0"]
